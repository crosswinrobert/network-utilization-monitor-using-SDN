# SDN Mininet Project – Orange Problem: Network Utilization Monitor

## Problem Statement
Implement an SDN-based solution using Mininet and POX controller that demonstrates:
- Controller-switch interaction (PacketIn, flow installation)
- Firewall rule (blocking specific traffic)
- Network utilization monitoring (bandwidth per port)

## Setup Instructions

### Prerequisites
- Ubuntu 20.04+ with Mininet installed
- POX controller

### Installation
1. Install Mininet:
   ```bash
   sudo apt install mininet -y
